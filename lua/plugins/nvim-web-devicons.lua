return {
  "nvim-tree/nvim-web-devicons",
  lazy = false,
  config = {
    color_icons = true,  -- different highlight color for different icons
    default = false,     -- globablly enable default icons
    strict = true,       -- strict icons by filename and its extensions
  }
}
